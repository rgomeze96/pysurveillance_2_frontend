import React from "react";
import { Page } from '../components/Page';

export const Contact = ( {name}) => {
    return (
        <Page title="Contact">
            Contact {name}
        </Page>
    )
}
